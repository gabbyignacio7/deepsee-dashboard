Add basic password protection to this React dashboard application.

**REQUIREMENTS:**

1. Create a login screen that appears BEFORE users can access the dashboard
2. Simple password-only authentication (no username)
3. Password stored as Replit Secret environment variable: DASHBOARD_PASSWORD
4. Store auth state in sessionStorage (expires when browser closes)
5. Add "Logout" button in dashboard header that clears session
6. Login screen should have:
   - "DeepSee Dashboard" heading
   - Password input field (type="password")
   - "Login" button
   - Error message for wrong password: "Incorrect password. Please try again."
   - Styled with dark blue/navy theme matching existing dashboard

**IMPLEMENTATION:**

Create `src/components/PasswordProtection.jsx`:

```jsx
import React, { useState, useEffect } from 'react';

export default function PasswordProtection({ children }) {
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');

  useEffect(() => {
    const authStatus = sessionStorage.getItem('dashboardAuth');
    if (authStatus === 'authenticated') {
      setIsAuthenticated(true);
    }
  }, []);

  const handleLogin = (e) => {
    e.preventDefault();
    if (password === import.meta.env.VITE_DASHBOARD_PASSWORD) {
      sessionStorage.setItem('dashboardAuth', 'authenticated');
      setIsAuthenticated(true);
      setError('');
    } else {
      setError('Incorrect password. Please try again.');
    }
  };

  if (isAuthenticated) {
    return children;
  }

  return (
    <div style={{
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#0f172a'
    }}>
      <div style={{
        backgroundColor: '#1e293b',
        padding: '2rem',
        borderRadius: '8px',
        width: '100%',
        maxWidth: '400px'
      }}>
        <h1 style={{ color: '#fff', marginBottom: '1.5rem', textAlign: 'center' }}>
          DeepSee Dashboard
        </h1>
        <form onSubmit={handleLogin}>
          <input
            type="password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="Enter password"
            style={{
              width: '100%',
              padding: '0.75rem',
              marginBottom: '1rem',
              borderRadius: '4px',
              border: '1px solid #334155',
              backgroundColor: '#0f172a',
              color: '#fff'
            }}
          />
          {error && <p style={{ color: '#ef4444', marginBottom: '1rem' }}>{error}</p>}
          <button
            type="submit"
            style={{
              width: '100%',
              padding: '0.75rem',
              backgroundColor: '#3b82f6',
              color: '#fff',
              border: 'none',
              borderRadius: '4px',
              cursor: 'pointer',
              fontWeight: 'bold'
            }}
          >
            Login
          </button>
        </form>
      </div>
    </div>
  );
}
```

**Update `src/App.jsx`** - wrap main app with PasswordProtection:

```jsx
import PasswordProtection from './components/PasswordProtection';

function App() {
  const handleLogout = () => {
    sessionStorage.removeItem('dashboardAuth');
    window.location.reload();
  };

  return (
    <PasswordProtection>
      {/* Add logout button in your header/nav */}
      <button onClick={handleLogout} style={{ /* your styles */ }}>
        Logout
      </button>
      
      {/* Rest of your dashboard components */}
    </PasswordProtection>
  );
}
```

**SETUP STEPS:**

1. Implement the code above
2. In Replit, go to Secrets (Tools > Secrets)
3. Add new secret: Key = `VITE_DASHBOARD_PASSWORD`, Value = `DeepSee2025!` (or your chosen password)
4. Restart the Repl for environment variable to load
5. Test login/logout functionality

**NOTES:**
- This is basic protection for internal team use
- SessionStorage clears when browser closes (users must re-login)
- Password will be shared separately with team via DM

Implement this now and confirm when complete.